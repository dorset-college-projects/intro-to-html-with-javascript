  // alert('Hello World');
  let greeting = 'Hello World';
  console.log(greeting);
  let personName = 'John';
  let personAge = 58;

  // The ` (backtick) allows you to interpolate the string using ${variable} syntax
  console.log(`${personName} is ${personAge} years old`)

  // old javascrip used to use the 'var' keyword, now we use let and const
  let century = 100;
s
  let yearsToCentry = century - personAge;

  console.log(`${personName} is ${personAge} years old and will be ${century} in ${yearsToCentry} years time`)

